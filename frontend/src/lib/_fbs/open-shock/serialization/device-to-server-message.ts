// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { DeviceToServerMessagePayload, unionToDeviceToServerMessagePayload, unionListToDeviceToServerMessagePayload } from '../../open-shock/serialization/device-to-server-message-payload';


export class DeviceToServerMessage {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):DeviceToServerMessage {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsDeviceToServerMessage(bb:flatbuffers.ByteBuffer, obj?:DeviceToServerMessage):DeviceToServerMessage {
  return (obj || new DeviceToServerMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsDeviceToServerMessage(bb:flatbuffers.ByteBuffer, obj?:DeviceToServerMessage):DeviceToServerMessage {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new DeviceToServerMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

payloadType():DeviceToServerMessagePayload {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : DeviceToServerMessagePayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startDeviceToServerMessage(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:DeviceToServerMessagePayload) {
  builder.addFieldInt8(0, payloadType, DeviceToServerMessagePayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, payloadOffset, 0);
}

static endDeviceToServerMessage(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishDeviceToServerMessageBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedDeviceToServerMessageBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createDeviceToServerMessage(builder:flatbuffers.Builder, payloadType:DeviceToServerMessagePayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  DeviceToServerMessage.startDeviceToServerMessage(builder);
  DeviceToServerMessage.addPayloadType(builder, payloadType);
  DeviceToServerMessage.addPayload(builder, payloadOffset);
  return DeviceToServerMessage.endDeviceToServerMessage(builder);
}
}
