// automatically generated by the FlatBuffers compiler, do not modify

/* eslint-disable @typescript-eslint/no-unused-vars, @typescript-eslint/no-explicit-any, @typescript-eslint/no-non-null-assertion */

import * as flatbuffers from 'flatbuffers';

import { GatewayToDeviceMessagePayload, unionToGatewayToDeviceMessagePayload, unionListToGatewayToDeviceMessagePayload } from '../../../open-shock/serialization/gateway/gateway-to-device-message-payload';


export class GatewayToDeviceMessage {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):GatewayToDeviceMessage {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsGatewayToDeviceMessage(bb:flatbuffers.ByteBuffer, obj?:GatewayToDeviceMessage):GatewayToDeviceMessage {
  return (obj || new GatewayToDeviceMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsGatewayToDeviceMessage(bb:flatbuffers.ByteBuffer, obj?:GatewayToDeviceMessage):GatewayToDeviceMessage {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new GatewayToDeviceMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

payloadType():GatewayToDeviceMessagePayload {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : GatewayToDeviceMessagePayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startGatewayToDeviceMessage(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:GatewayToDeviceMessagePayload) {
  builder.addFieldInt8(0, payloadType, GatewayToDeviceMessagePayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, payloadOffset, 0);
}

static endGatewayToDeviceMessage(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishGatewayToDeviceMessageBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedGatewayToDeviceMessageBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createGatewayToDeviceMessage(builder:flatbuffers.Builder, payloadType:GatewayToDeviceMessagePayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  GatewayToDeviceMessage.startGatewayToDeviceMessage(builder);
  GatewayToDeviceMessage.addPayloadType(builder, payloadType);
  GatewayToDeviceMessage.addPayload(builder, payloadOffset);
  return GatewayToDeviceMessage.endGatewayToDeviceMessage(builder);
}
}
