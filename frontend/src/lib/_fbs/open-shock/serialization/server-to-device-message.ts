// automatically generated by the FlatBuffers compiler, do not modify

import * as flatbuffers from 'flatbuffers';

import { ServerToDeviceMessagePayload, unionToServerToDeviceMessagePayload, unionListToServerToDeviceMessagePayload } from '../../open-shock/serialization/server-to-device-message-payload';


export class ServerToDeviceMessage {
  bb: flatbuffers.ByteBuffer|null = null;
  bb_pos = 0;
  __init(i:number, bb:flatbuffers.ByteBuffer):ServerToDeviceMessage {
  this.bb_pos = i;
  this.bb = bb;
  return this;
}

static getRootAsServerToDeviceMessage(bb:flatbuffers.ByteBuffer, obj?:ServerToDeviceMessage):ServerToDeviceMessage {
  return (obj || new ServerToDeviceMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

static getSizePrefixedRootAsServerToDeviceMessage(bb:flatbuffers.ByteBuffer, obj?:ServerToDeviceMessage):ServerToDeviceMessage {
  bb.setPosition(bb.position() + flatbuffers.SIZE_PREFIX_LENGTH);
  return (obj || new ServerToDeviceMessage()).__init(bb.readInt32(bb.position()) + bb.position(), bb);
}

payloadType():ServerToDeviceMessagePayload {
  const offset = this.bb!.__offset(this.bb_pos, 4);
  return offset ? this.bb!.readUint8(this.bb_pos + offset) : ServerToDeviceMessagePayload.NONE;
}

payload<T extends flatbuffers.Table>(obj:any):any|null {
  const offset = this.bb!.__offset(this.bb_pos, 6);
  return offset ? this.bb!.__union(obj, this.bb_pos + offset) : null;
}

static startServerToDeviceMessage(builder:flatbuffers.Builder) {
  builder.startObject(2);
}

static addPayloadType(builder:flatbuffers.Builder, payloadType:ServerToDeviceMessagePayload) {
  builder.addFieldInt8(0, payloadType, ServerToDeviceMessagePayload.NONE);
}

static addPayload(builder:flatbuffers.Builder, payloadOffset:flatbuffers.Offset) {
  builder.addFieldOffset(1, payloadOffset, 0);
}

static endServerToDeviceMessage(builder:flatbuffers.Builder):flatbuffers.Offset {
  const offset = builder.endObject();
  return offset;
}

static finishServerToDeviceMessageBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset);
}

static finishSizePrefixedServerToDeviceMessageBuffer(builder:flatbuffers.Builder, offset:flatbuffers.Offset) {
  builder.finish(offset, undefined, true);
}

static createServerToDeviceMessage(builder:flatbuffers.Builder, payloadType:ServerToDeviceMessagePayload, payloadOffset:flatbuffers.Offset):flatbuffers.Offset {
  ServerToDeviceMessage.startServerToDeviceMessage(builder);
  ServerToDeviceMessage.addPayloadType(builder, payloadType);
  ServerToDeviceMessage.addPayload(builder, payloadOffset);
  return ServerToDeviceMessage.endServerToDeviceMessage(builder);
}
}
