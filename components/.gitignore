# IDF build artifacts generated by `idf.py` in per-component host tests
# (e.g. components/*/host_test). Applies to every component subdirectory.
# managed_components/ and dependencies.lock are already ignored repo-wide.
build/
sdkconfig
sdkconfig.old
