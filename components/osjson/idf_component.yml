# OpenShock JSON handling (util/Json.h) is built on these espressif components.
# Declared here explicitly rather than relying on them being pulled in as
# transitive private dependencies of other components.
dependencies:
  espressif/jsmn: "^1.1.0"
  espressif/json_generator: "^1.1.2"
